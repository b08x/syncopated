---
# tasks file for shell

- name: Set user shell
  user:
    name: "{{ item }}"
    shell: "{{ user.shell }}"
  with_items:
    - "{{ user.name }}"
    - root
  tags: ['profile']

- name: Import zsh role
  import_role:
    name: zsh
  tags: ['zsh', 'profile']
